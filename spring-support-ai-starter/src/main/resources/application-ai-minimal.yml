spring:
  ai:
    performance:
      enabled: true
      api-enabled: true
      sample-count: 3

    # LLM 配置（最小配置）
    llm:
      provider: openai
      model: gpt-4
    
    # 云厂商配置
    providers:
      openai:
        api-key: ${OPENAI_API_KEY}
