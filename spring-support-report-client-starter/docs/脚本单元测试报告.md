# 脚本单元测试报告

## 1. 本次执行说明

- 执行日期：2026-03-26
- 执行环境：`192.168.110.100` / `CentOS 7`
- 执行账号：`root`
- Java：`/opt/jdk-25/bin/java`
- 脚本入口：`spring-support-report-client-starter/src/test/resources/test_all_scripts.sh`
- 执行模式：`MATRIX_MODE=mock`

说明：

- 当前仓库没有可直接部署的 `start.sh` / `stop.sh` / `restart.sh` / `gui.sh` 成品脚本，因此本次脚本矩阵采用 `mock` 模式。
- `mock` 模式会在远程 Linux 现场生成最小可执行 HTTP 健康检查 jar，以及 `start.sh`、`status.sh`、`restart.sh`、`stop.sh`、`health.sh`、`upgrade.sh`、`rollback.sh`、`gui.sh` 八个脚本，再对命令行为做矩阵验证。
- 报告中的矩阵名称、结果、日志路径与 `test_all_scripts.sh` 实际输出一一对应，没有填写未执行项。

## 2. 执行命令

语法检查：

```bash
cd /opt/workspace/spring-support-parent-starter
MATRIX_MODE=mock JAVA_BIN=/opt/jdk-25/bin/java JAVAC_BIN=/opt/jdk-25/bin/javac JAR_BIN=/opt/jdk-25/bin/jar \
  bash -n spring-support-report-client-starter/src/test/resources/test_all_scripts.sh
```

完整矩阵：

```bash
cd /opt/workspace/spring-support-parent-starter
MATRIX_MODE=mock JAVA_BIN=/opt/jdk-25/bin/java JAVAC_BIN=/opt/jdk-25/bin/javac JAR_BIN=/opt/jdk-25/bin/jar \
  bash spring-support-report-client-starter/src/test/resources/test_all_scripts.sh
```

关键输出：

```text
SYNTAX_OK
SUMMARY|MODE=mock|TOTAL=19|PASS=19|FAIL=0|LOG_DIR=/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix
```

## 3. 覆盖统计

- 命令脚本覆盖：8 个
- 场景覆盖：19 个
- 正向场景：10 个
- 失败路径场景：9 个
- 包格式覆盖：3 个

命令脚本覆盖范围：

1. `start.sh`
2. `status.sh`
3. `restart.sh`
4. `stop.sh`
5. `health.sh`
6. `upgrade.sh`
7. `rollback.sh`
8. `gui.sh`

场景覆盖范围：

1. localhost + context-path 多节点启动
2. localhost + context-path 多节点状态查询
3. localhost 单节点重启
4. localhost 单节点停止
5. localhost + path 健康检查
6. 重复启动失败
7. 缺少端口参数启动失败
8. `status.sh` 缺少目录参数失败
9. `stop.sh` 缺少目录参数失败
10. `health.sh` 缺少运行环境失败
11. `restart.sh` 缺少运行环境失败
12. jar 升级 + 回滚
13. zip 资源分离包升级 + 回滚
14. tar.gz 资源分离包升级 + 回滚
15. 升级包缺失失败
16. 回滚点缺失失败
17. `start.sh -j` 自定义 Java 路径
18. `gui.sh --self-test` 快捷键映射
19. `gui.sh` 非法参数失败

## 4. 场景矩阵

| 场景 ID | 说明 | 结果 | 远程日志 |
| --- | --- | --- | --- |
| `LOCALHOST_MULTI_NODE_START` | 双节点、多端口、localhost + context-path 启动 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/LOCALHOST_MULTI_NODE_START.log` |
| `LOCALHOST_MULTI_NODE_STATUS` | 双节点状态查询 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/LOCALHOST_MULTI_NODE_STATUS.log` |
| `LOCALHOST_MULTI_NODE_RESTART` | 单节点重启后 PID 变化且健康恢复 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/LOCALHOST_MULTI_NODE_RESTART.log` |
| `LOCALHOST_MULTI_NODE_STOP` | 单节点停止后状态为 STOPPED | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/LOCALHOST_MULTI_NODE_STOP.log` |
| `LOCALHOST_MULTI_NODE_HEALTH` | 健康检查 URL 包含 context-path 与自定义 base-path | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/LOCALHOST_MULTI_NODE_HEALTH.log` |
| `START_DUPLICATE_FAIL` | 同一节点重复启动返回失败 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/START_DUPLICATE_FAIL.log` |
| `START_MISSING_PORT_FAIL` | 启动缺少端口参数返回失败 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/START_MISSING_PORT_FAIL.log` |
| `STATUS_MISSING_HOME_FAIL` | 状态查询缺少目录参数返回失败 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/STATUS_MISSING_HOME_FAIL.log` |
| `STOP_MISSING_HOME_FAIL` | 停止命令缺少目录参数返回失败 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/STOP_MISSING_HOME_FAIL.log` |
| `HEALTH_MISSING_ENV_FAIL` | 健康检查缺少运行环境文件返回失败 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/HEALTH_MISSING_ENV_FAIL.log` |
| `RESTART_MISSING_ENV_FAIL` | 重启命令缺少运行环境文件返回失败 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/RESTART_MISSING_ENV_FAIL.log` |
| `UPGRADE_JAR_ROLLBACK` | jar 直替升级并回滚到 v1 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/UPGRADE_JAR_ROLLBACK.log` |
| `UPGRADE_ZIP_RESOURCE_ROLLBACK` | zip 资源分离包升级并回滚到基线配置 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/UPGRADE_ZIP_RESOURCE_ROLLBACK.log` |
| `UPGRADE_TARGZ_RESOURCE_ROLLBACK` | tar.gz 资源分离包升级并回滚到基线配置 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/UPGRADE_TARGZ_RESOURCE_ROLLBACK.log` |
| `UPGRADE_MISSING_PACKAGE_FAIL` | 升级包不存在时返回失败 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/UPGRADE_MISSING_PACKAGE_FAIL.log` |
| `ROLLBACK_MISSING_ID_FAIL` | 没有回滚点时返回失败 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/ROLLBACK_MISSING_ID_FAIL.log` |
| `START_CUSTOM_JAVA_PATH` | `start.sh -j /opt/jdk-25/bin/java` 生效 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/START_CUSTOM_JAVA_PATH.log` |
| `GUI_SELF_TEST` | `gui.sh --self-test` 快捷键映射验证 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/GUI_SELF_TEST.log` |
| `GUI_INVALID_ARG_FAIL` | `gui.sh` 非法参数返回失败 | PASS | `/opt/workspace/spring-support-parent-starter/spring-support-report-client-starter/target/script-matrix/GUI_INVALID_ARG_FAIL.log` |

## 5. 关键输出摘要

`LOCALHOST_MULTI_NODE_START`：

```text
STARTED|PID=11027|URL=http://127.0.0.1:19180/cluster-a/actuator-a/health
STARTED|PID=11062|URL=http://127.0.0.1:19181/cluster-b/actuator-b/health
```

`START_CUSTOM_JAVA_PATH` 运行后落盘参数：

```text
PORT=19190
NODE_NAME=java-a
CONTEXT_PATH=/java-a
ACTUATOR_BASE=/manage
JAVA_CMD=/opt/jdk-25/bin/java
```

`GUI_SELF_TEST` 输出：

```text
CTRL+R=restart
CTRL+S=stop
CTRL+H=health
CTRL+U=upgrade
SELF_TEST=PASS
```

`UPGRADE_ZIP_RESOURCE_ROLLBACK` 回滚后配置恢复：

```text
mode=baseline
```

失败路径样例：

```text
START_DUPLICATE_FAIL -> already running
START_MISSING_PORT_FAIL -> missing port
HEALTH_MISSING_ENV_FAIL -> missing env file
UPGRADE_MISSING_PACKAGE_FAIL -> missing package file
ROLLBACK_MISSING_ID_FAIL -> missing rollback id
GUI_INVALID_ARG_FAIL -> usage: gui.sh --self-test
```

## 6. 失败项与修复项

本轮远程矩阵失败项：

- 无。19 个场景全部通过。

执行过程修复项：

1. 将 `test_all_scripts.sh` 默认模式调整为 `MATRIX_MODE=mock`，改为远程现场生成最小 mock jar 和八个脚本，只验证脚本命令行为。
2. 保留原 JUnit 矩阵为 `MATRIX_MODE=junit` 备用，不再作为远程脚本测试主路径。
3. 修复 `run_junit_matrix` 的空数组展开逻辑，避免 `set -u` 下误报。
4. 新增 9 个失败路径场景，补齐启动、状态、停止、健康检查、重启、升级、回滚、GUI 参数错误等命令异常分支。

## 7. 结论

- 本次远程脚本矩阵共执行 19 个场景，全部通过。
- 命令级脚本验证已经覆盖正向链路和主要失败路径。
- 多节点多端口启动、状态、重启、停止、健康检查，jar/zip/tar.gz 升级回滚，`start.sh -j` 自定义 Java 路径，以及 `gui.sh --self-test` 快捷键映射均已完成远程实跑验证。
