<?xml version="1.0" encoding="UTF-8"?>
<!--
    Spring Support Swagger2 Starter - Swagger 2.x 兼容模块

    提供 Swagger 2.x 注解支持，用于兼容使用老版本 Swagger 的项目。
    该模块仅提供 Swagger 2.x 的注解依赖，不包含 Swagger UI 等运行时组件。

    使用场景：
    - 需要兼容使用 @Api、@ApiOperation、@ApiParam 等 Swagger 2.x 注解的老项目
    - 与 spring-support-swagger-starter（Swagger 3.x）可以同时使用
    - 通过反射机制，common-starter 中的日志组件可以自动识别 Swagger 2.x 注解

    @author CH
    @version 4.0.0.36
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.chua</groupId>
        <artifactId>spring-support-parent-starter</artifactId>
        <version>4.0.0.36</version>
        
    </parent>

    <artifactId>spring-support-swagger2-starter</artifactId>
    <description>Swagger 2.x 兼容模块，提供 Swagger 2.x 注解支持</description>

    <properties>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <!-- Swagger 2.x 注解 - 用于 API 文档注解（兼容老版本） -->
        <dependency>
            <groupId>io.swagger</groupId>
            <artifactId>swagger-annotations</artifactId>
            
            <scope>provided</scope>
        </dependency>

        <!-- Spring Boot自动配置 - 提供自动配置功能 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                
                <configuration>
                    <source>25</source>
                    <target>25</target>
                    <encoding>UTF-8</encoding>
                    <proc>full</proc>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>

