# 软件单元测试文档

## 功能说明

| 项目 | 说明 |
| --- | --- |
| 组件定位 | `spring-support-soft-starter` 是独立的软件管理模块，负责软件仓库、软件目录、安装引导、安装实例、服务控制、日志与配置管理 |
| 联调宿主 | `spring-api-support-soft-test-starter` |
| 后端入口 | `http://127.0.0.1:18171/soft-test/api` |
| 前端入口 | `http://127.0.0.1:8856/#/soft/*`、`http://127.0.0.1:8856/#/server/list` |
| 验证方式 | 真实 HTTP、真实数据库、真实 SSH 目标、真实浏览器页面、真实进程与真实健康检查 |
| 验证边界 | 不依赖 `system`、`monitor`，不写 mock，不新增测试代码；本轮重点收口 `soft-test` 独立壳、文件驱动软件录入、服务器页直装、自动跳详情与远程 Linux 真实安装链路 |

## 联调环境

| 项目 | 值 |
| --- | --- |
| 验证日期 | `2026-04-07`（`soft-test` 真实联调） |
| 补充核验日期 | `2026-04-14`（当前仓库代码核对 + 定向单测） |
| soft-test 服务 | `http://127.0.0.1:18171/soft-test/api` |
| 前端测试壳 | `http://127.0.0.1:8856` |
| 本机静态源 | `http://127.0.0.1:18901` |
| 浏览器主工具 | `agent-browser 0.21.4` |
| 页面联调方式 | `agent-browser` 已恢复可用，使用独立 session（`softrecords`、`softcatalog`、`softsrv`）逐页联调，避免同一 session 并发导致的卡死 |
| 本机目标 | `LOCAL / WINDOWS / 127.0.0.1 / H:/workspace/2/tmp/soft-runtime/local` |
| 远程目标 | `SSH / LINUX / 192.168.110.100:22 / root / /opt/soft-runtime` |

## 当前真实对象

### 服务器 / 目标

| 类型 | 名称 | ID | 关键值 |
| --- | --- | --- | --- |
| 服务器 | `本机 Windows` | `serverId=1` | `LOCAL / WINDOWS / 127.0.0.1 / yemen` |
| 服务器 | `远程 Linux 192.168.110.100` | `serverId=2` | `SSH / LINUX / 192.168.110.100 / root` |
| 目标 | `本机 Windows` | `softTargetId=1` | `LOCAL / WINDOWS / H:/workspace/2/tmp/soft-runtime/local` |
| 目标 | `远程 Linux 192.168.110.100` | `softTargetId=2` | `SSH / LINUX / /opt/soft-runtime` |

### 软件源定义

| 编号 | 编码 | 类型 | 状态 | 说明 |
| --- | --- | --- | --- | --- |
| `1` | `real-soft-repo` | `LOCAL_DIR` | `SUCCESS` | 综合软件源，内含本地目录、在线索引、RPM 目录、镜像目录等多源定义 |
| `2` | `remote-artifacts-http` | `HTTP_DIR` | `SUCCESS` | 远程 HTTP 安装包目录 |
| `3` | `rpm-third-party` | `RPM_REPO` | `SUCCESS` | 第三方 RPM 源 |
| `4` | `mirror-third-party` | `MIRROR_REPO` | `SUCCESS` | 第三方镜像安装包源 |

### 综合软件源的多源状态

| 源名称 | 类型 | 启用 | 地址 |
| --- | --- | --- | --- |
| 主索引源 | `HTTP_JSON` | 是 | `http://127.0.0.1:18901/soft-repo-main/index.json` |
| 补充索引源 | `HTTP_JSON` | 是 | `http://127.0.0.1:18901/soft-repo-extra/index.json` |
| RPM 远程源 | `RPM_REPO` | 是 | `http://127.0.0.1:18901/soft-artifacts/rpm/` |
| 镜像目录源 | `MIRROR_REPO` | 是 | `http://127.0.0.1:18901/soft-artifacts/mirror/` |
| 禁用镜像目录 | `MIRROR_REPO` | 否 | `http://127.0.0.1:18901/soft-artifacts/disabled/` |

### 软件包

| 软件包 ID | 包编码 | 名称 | 平台 | `softwareKey` |
| --- | --- | --- | --- | --- |
| `20` | `minio` | `MinIO Linux AMD64` | `LINUX / amd64` | `minio` |
| `12` | `minio` | `MinIO Windows AMD64` | `WINDOWS / amd64` | `minio` |
| `13` | `mysql-community` | `Mysql Community` | `LINUX / amd64` | `mysql-community` |
| `14` | `nacos-server` | `Nacos Server` | `LINUX / amd64` | `nacos-server` |
| `16` | `nginx` | `Nginx` | `LINUX / amd64` | `nginx` |
| `21` | `nginx` | `Nginx` | `LINUX / amd64` | `nginx` |
| `17` | `redis` | `Redis` | `LINUX / amd64` | `redis` |
| `22` | `redis` | `Redis` | `LINUX / amd64` | `redis` |
| `15` | `opspilot` | `Opspilot` | `WINDOWS / amd64` | `opspilot` |
| `11` | `example-disabled` | `Example Disabled` | `LINUX / amd64` | `example-disabled` |

### 当前安装实例

| 实例 | 安装 ID | 包 ID | 目标 | 路径 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `Opspilot-Retry` | `1` | `15` | 本机 Windows `softTargetId=1` | `H:/workspace/2/tmp/soft-runtime/local/opspilot/retry-1.0.1` | `FAILED / UNKNOWN` |

### 当前操作记录

| 操作号 | 实例 | 类型 | 结果 | 时间 |
| --- | --- | --- | --- | --- |
| `1` | `Opspilot-Retry` | `INSTALL` | `FAILED / FINISH / 100% / 执行失败` | `03:47:24 ~ 03:47:27` |

## 文件驱动录入与目录聚合验证

### 录入口径

| 项目 | 结果 |
| --- | --- |
| 主录入口 | `POST /soft/repositories/{id}/artifacts/upload` |
| 目录型源 | `LOCAL_DIR / HTTP_DIR / RPM_REPO / MIRROR_REPO` 已真实入库 |
| 版本元数据 | 真实版本数据已包含 `artifactKind / softwareKey / artifactPath / artifactExtension` 等补充元信息 |
| 目录聚合键 | 前端按后端 `softwareKey` 聚合同软件不同操作系统版本 |
| 聚合结果 | `MinIO Linux + Windows` 在目录页聚合为同一软件卡片 |

### 本轮实际样例

| 样例 | 实际结果 |
| --- | --- |
| `opspilot-1.0.1-windows-amd64.zip` | 已真实入库为 `packageCode=opspilot`，目录页显示为 `Opspilot` |
| `mysql-community-8.4.3-linux-amd64.rpm` | 已真实入库为 `Mysql Community` |
| `nacos-server-2.4.0-linux-amd64.tar.gz` | 已真实入库为 `Nacos Server` |
| `nginx-1.26.3-linux-amd64.rpm` | 已从多源同步出两条包记录，目录聚合后仍归同一软件 |
| `redis-7.4.2-linux-amd64.rpm` | 已从多源同步出两条包记录，目录聚合后仍归同一软件 |

## 服务器页直装与失败落账闭环验证

### 页面验证结果

| 验证项 | 入口 | 实际结果 |
| --- | --- | --- |
| 服务器列表页加载 | `#/server/list` | 页面可正常打开，显示 `2` 台服务器 |
| 本机 Windows 安装按钮 | 本机服务器卡片工具栏 | 可打开“安装软件到 本机 Windows”弹窗 |
| 第一步软件选择 | 服务器页安装弹窗 | 只显示与 `WINDOWS / AMD64` 匹配的软件，真实出现 `MinIO Windows AMD64`、`Opspilot` |
| 第二步版本与引导参数 | 同弹窗 | 版本、实例名称、安装路径、环境参数真实渲染，默认版本为 `1.0.1` |
| 提交安装 | 同弹窗 | `POST /soft/installations` 返回 `200`，创建本机安装实例 |
| 自动跳详情 | 提交后 | 页面自动跳到 `#/soft/detail/15?installationId=1`，未停留在服务器页 |
| 失败结果展示 | `#/soft/detail/15?installationId=1`、`#/soft/records` | 详情页与记录页均显示 `INSTALL / 失败 / FINISH / 100 / 执行失败`，无需手工刷新 |

### 本轮服务器页直装参数

| 参数 | 值 |
| --- | --- |
| `softPackageId` | `15` |
| `softPackageVersionId` | `24` |
| `softTargetId` | `1` |
| `installationName` | `Opspilot-Retry` |
| `installPath` | `H:/workspace/2/tmp/soft-runtime/local/opspilot/retry-1.0.1` |
| `serviceName` | `opspilot-retry` |
| `environmentName` | `prod` |
| 安装包样例 | `opspilot-1.0.1-windows-amd64.zip`（故意保留的无效 ZIP，用于验证 Windows 失败落账语义） |

### 本轮服务器页直装后端结果

| 项目 | 结果 |
| --- | --- |
| 安装实例 | `softInstallationId=1` |
| 安装记录 | `softOperationLogId=1`，`INSTALL / FAILED / FINISH / 100%` |
| 安装状态 | `FAILED` |
| 运行状态 | `UNKNOWN` |
| 失败摘要 | `执行失败` |
| 失败关键输出 | `Expand-Archive`、`End of Central Directory record could not be found.` |

### 远程 Linux 运行基线

| 项目 | 结果 |
| --- | --- |
| 目标主机 | `192.168.110.100` |
| 既有运行实例 | `MinIO` |
| 健康检查 | `http://192.168.110.100:19050/minio/health/live` 返回 `200` |
| 说明 | 当前数据库快照未回灌这条远程实例，但远程运行态仍真实存在，作为 `SSH / LINUX` 不回归基线保留 |

## 关键修复结论

| 修复点 | 位置 | 结果 |
| --- | --- | --- |
| 服务器页安装选择器绑定到高级 `ScSelect` | `packages/components/ScSelect`、`pages/server/src/views/ServerHostPage.vue` | 安装第一步已恢复为真实列表选择，不再落到轻量 wrapper |
| 安装向导中的 `softPackageId / versionId` 数值归一化 | `pages/server/src/views/ServerHostPage.vue`、`pages/soft/src/views/SoftCatalogPage.vue` | 第一步选包后“下一步”能正确解锁，不再被 `string/number` 混用卡住 |
| 服务器页提交后跳详情竞态 | `pages/server/src/views/ServerHostPage.vue` | 真实安装提交后已稳定跳到对应 `soft/detail`，本轮复验为 `#/soft/detail/15?installationId=1` |
| Windows PowerShell 非终止错误落账 | `spring-support-soft-starter/.../SoftCommandSupport.java` | 通过强制 `ErrorActionPreference=Stop`、显式检查 `LASTEXITCODE` 与 `catch exit 1`，本机无效 ZIP 安装现已真实落为 `FAILED`，不再误记为 `SUCCESS` |

## 真实验收矩阵

| 类别 | 验证项 | 结果 |
| --- | --- | --- |
| 软件源 | 多源同步 | 通过，综合软件源 `5 成功 + 1 禁用/跳过` |
| 软件源 | `LOCAL_DIR` 目录录入 | 通过 |
| 软件源 | `HTTP_DIR` 目录录入 | 通过 |
| 软件源 | `RPM_REPO` 目录录入 | 通过 |
| 软件源 | `MIRROR_REPO` 目录录入 | 通过 |
| 目录 | 同软件跨 OS 聚合 | 通过，`MinIO Linux + Windows` 聚合为同一卡片 |
| 目录 | 文件驱动新增软件 | 通过，`Opspilot` 已真实出现于目录页 |
| 服务器 | 服务器列表联动 soft | 通过，服务器页可直接打开安装向导 |
| 安装 | 服务器页直装 | 通过 |
| 安装 | 自动跳软件详情 | 通过 |
| 安装 | Windows 本地无效安装包失败落账 | 通过，`Opspilot-Retry` 真实显示 `FAILED / FINISH / 100%` |
| 页面 | `#/soft/records` 实时显示失败安装 | 通过，页面正文真实显示 `INSTALL / 失败 / FINISH / 100 / 执行失败` |
| 运行 | 远程 Linux MinIO 启动 | 通过 |
| 运行 | 远程 Linux 状态检查 | 通过，输出 `active` |
| 运行 | 远程 Linux 健康检查 | 通过，HTTP `200` |

## 2026-04-14 补充核验

| 类别 | 验证项 | 结果 |
| --- | --- | --- |
| 代码核对 | AI 草稿接口入口 | 通过，`SoftPackageController` 已存在 `POST /soft/packages/ai-draft` |
| 单元测试 | `SoftPackageAiDraftAdvisorTest` | 通过，执行 `mvn -pl spring-support-soft-starter -Dtest=SoftPackageAiDraftAdvisorTest -DskipTests=false test` 返回 `BUILD SUCCESS` |
| 文档一致性 | 任务计划与验收文档 | 通过，`软管理前后端联动完整任务计划.md` 已同步回写 Step 7 ~ Step 9 完成状态 |
| 边界说明 | `soft-test` 壳工程源码 | 当前工作区未包含 `spring-api-support-soft-test-starter` / `vue-support-soft-test-starter` 源码，本轮不重复启动 `18171 + 8856`，仅补充仓库侧证据链 |

## 本轮真实操作记录

| 时间 | 操作 | 结果 |
| --- | --- | --- |
| `2026-04-07 03:43` | 首次在本机 Windows 重放 `Opspilot` 无效 ZIP 安装 | 日志已出现 `Expand-Archive` 错误，但当时后端仍误落为 `SUCCESS`，定位到 PowerShell 非终止错误语义问题 |
| `2026-04-07 03:45 ~ 03:46` | 重新构建 `spring-support-soft-starter` 与 `spring-api-support-soft-test-starter`，并通过计划任务 `CodexSoftTestApi` 重启后端 | 后端恢复到 `18171`，修复代码生效 |
| `2026-04-07 03:47:24 ~ 03:47:27` | 再次从服务器页对本机 Windows 提交 `Opspilot-Retry` 安装 | 生成 `softInstallationId=1`，操作 `softOperationLogId=1` 真实结束为 `FAILED / FINISH / 100%` |
| `2026-04-07 03:47:27` | 查询 `GET /soft/installations` 与 `GET /soft/operations` | 当前数据库只保留 `Opspilot-Retry` 与对应失败安装记录 |
| `2026-04-07 03:57:36` | 使用 `agent-browser` 打开 `#/soft/detail/15?installationId=1` 并读取正文 | 页面真实显示 `安装状态=失败`、`运行状态=未知`、安装/服务/配置参数摘要与模板落地路径 |
| `2026-04-07 03:54:12` | 使用 `agent-browser` 打开 `#/soft/records` 并读取正文 | 页面真实显示 `INSTALL / 失败 / FINISH / 100 / 执行失败`，失败结果已同步到记录页 |
| `2026-04-07 02:44:53` | 服务器页为远程 Linux 提交 MinIO 安装 | 生成安装 `softInstallationId=2` |
| `2026-04-07 02:44:59` | 安装完成 | 操作 `softOperationLogId=2` 成功结束 |
| `2026-04-07 02:44:58 ~ 02:45:00` | 页面自动跳转到 `#/soft/detail/20?installationId=2` | 跳转成功，详情参数正常显示 |
| `2026-04-07 02:46:57 ~ 02:46:59` | 启动新实例 `minio-latest-linux-19050` | 操作 `softOperationLogId=4` 成功，输出 `active` |
| `2026-04-07 02:47:01 ~ 02:47:02` | 首次状态检查 | 操作 `softOperationLogId=5` 成功，输出 `active` |
| `2026-04-07 02:47:17 ~ 02:47:18` | 二次状态检查 | 操作 `softOperationLogId=6` 成功，输出 `active` |
| `2026-04-07 02:47:xx` | 健康检查 `http://192.168.110.100:19050/minio/health/live` | 返回 `200` |
| `2026-04-14 15:19` | 在当前仓库重新执行 `SoftPackageAiDraftAdvisorTest` | 命令 `mvn -pl spring-support-soft-starter -Dtest=SoftPackageAiDraftAdvisorTest -DskipTests=false test` 返回 `BUILD SUCCESS` |
| `2026-04-14 15:22` | 核对 AI 草稿控制器入口与任务计划状态 | 已确认 `SoftPackageController` 存在 `/soft/packages/ai-draft`，并已同步回写任务计划文档 |

## 当前遗留项

- 当前 `soft-test` 数据库快照只保留了本机 Windows 的 `Opspilot-Retry` 失败实例；远程 Linux MinIO 运行态仍真实存在，但实例数据未回灌到当前库，因此“远程运行正常”与“当前库中无对应实例”需要分开理解。
- `opspilot-1.0.1-windows-amd64.zip` 是故意保留的无效样例，用于持续验证 Windows 安装失败语义；后续如果要恢复 Windows 成功安装基线，需要再补一份可正常解压的真实安装包。
- 服务器页和详情页当前已完成独立闭环，但“备份点 / 升级 / 现代化控制台重构 / 编辑器增强”仍未进入本轮交付范围。
- 当前工作区未包含 `spring-api-support-soft-test-starter` / `vue-support-soft-test-starter` 源码；后续若继续调整 `SoftPackageCreateRequest`、`/soft/packages/ai-draft` 或 `pages/soft` 表单交互，需要回到 `18171 + 8856` 环境重新做一次完整回归。

## 结论

- `soft` 现已以 `soft-test` 独立壳完成可联调闭环，正式联调入口为 `18171 + 8856`。
- 文件驱动软件录入已经打通，软件源支持 `LOCAL_DIR / HTTP_DIR / RPM_REPO / MIRROR_REPO` 多类型、多源同步，目录聚合也已经按 `softwareKey` 生效。
- 服务器页直装已真实打通：
  - 从 `#/server/list` 打开安装向导
  - 选择与服务器匹配的软件
  - 提交后真实创建安装实例
  - 页面自动跳到对应 `soft/detail`
  - 失败时也能正确在详情页和记录页落账
- 当前最关键的新结论是：Windows PowerShell 的非终止错误现已被强制收口为真实失败结果，`Expand-Archive` 这类错误不再把安装误记为 `SUCCESS`，而是会真实落成 `FAILED / FINISH / 100%`。
- `2026-04-14` 已对 AI 草稿链路补做仓库侧验证：`/soft/packages/ai-draft` 接口入口在位，`SoftPackageAiDraftAdvisorTest` 重新执行通过，任务计划与验收文档已同步一致。
