# 策略模块单元与模块测试报告

## 1. 执行信息

- 模块：`spring-support-strategy-starter`
- 统计日期：`2026-03-26`
- 当前仓库内直接测试来源：
  - 模块内源码级单元测试：`0`
  - 历史最小 Spring Boot 验证工程：`1` 个测试类，`7` 个用例

## 2. 当前已执行测试

| 测试位置 | 测试类 | 用例数 | 结果 | 说明 |
| --- | --- | ---: | --- | --- |
| 历史最小 Spring Boot 验证工程 | `SmokeModuleContextTest` | 7 | 通过 | 以简单 Spring Boot 项目验证策略模块装配、控制台认证和指标接口 |

## 3. 已验证事项

- 无 Redis 运行时依赖时，策略 starter 仍可正常启动。
- 嵌入式认证接口可返回统一响应。
- 控制台页面未登录时会跳转登录页。
- 控制台 API 未登录时返回 `401`。
- 登录后 `GET /v2/strategy/metrics` 可正常返回。
- 控制面接口已从业务策略拦截链排除，不再被限流、熔断、防护规则反向影响。
- 熔断配置运行时查表已支持 fail-open，不会因为配置表暂不可用直接放大成业务 `500`。

## 4. 当前测试缺口

- `spring-support-strategy-starter` 模块目录下尚无独立 JUnit 单元测试类。
- 未覆盖 Redis 分布式限流、防抖、配置同步。
- 未覆盖数据库配置表 CRUD 与规则匹配明细。
- 未覆盖控制台前端页面自动化测试。
- 未覆盖真实多节点配置广播与恢复场景。

## 5. 建议补测顺序

1. 为 `StrategyMetricsController` 增加返回结构与回退逻辑单测。
2. 为 `SysCircuitBreakerConfigurationServiceImpl` 增加配置表不可用 fail-open 单测。
3. 为 `StrategyWebMvcConfiguration` 增加控制面排除路径装配测试。
4. 在引入 Redis 的测试 profile 下补充分布式限流、防抖、配置同步集成测试。
